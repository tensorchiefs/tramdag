{
  "_note": "Protocol = the paper's R code 1:1 up to the CI batch/lr deviation (see the YAML header): As triangle-atan-cs with a second nonlinear f. sin has two turning points; cs_curve_max_abs_err is scored on paper Fig. 18's window, x2 in [-1, 0]. mean_x3_flow_do_x1 stays unpinned on purpose: the DGP center plus the abs-err bound covers it. hidden_features_CS = c(2,25,25,2) reads as in/out dims around the hidden stack (25,25); {max} bounds are 2.5x the measurement.",
  "beta12": {
    "value": 1.9808403253555298,
    "atol": 0.05
  },
  "beta13": {
    "value": -0.16783329844474792,
    "atol": 0.05,
    "why": "weakly identified: the coefficient multiplies the mixture x1 (sd 0.254 vs unit-scale siblings) \u2014 see the _note and docs/paper-replication.md"
  },
  "val_nll_x3": {
    "value": 2.461359739303589,
    "atol": 0.05
  },
  "cs_curve_max_abs_err": {
    "max": 0.6006
  },
  "mean_x3_dgp_do_x1": {
    "value": 2.1092599399621044,
    "atol": 1e-06,
    "why": "a deterministic seeded DGP draw, like the other analytic entries; only numpy's last-bit drift moves it"
  },
  "mean_x3_abs_err_do_x1": {
    "max": 0.188
  },
  "fit_seconds": {
    "max": 957,
    "why": "3x the 2-core CI runner measurement (319 s, tuned protocol): shared-runner wall clock is noisy, so this is a gross-regression tripwire, not a benchmark"
  }
}
